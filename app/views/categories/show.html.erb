<h2><%= @category.name.capitalize %></h2>
<h4><%= link_to 'New Article', new_category_article_path(@category) %></h4>
<% @category.articles.each do |article| %>
  <p><%= link_to article.title, [@category, article] %></p>
<% end %>
